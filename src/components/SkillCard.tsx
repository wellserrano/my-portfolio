import * as Progress from '@radix-ui/react-progress';

interface SkillCardProps {
  language: string
  level: number
}

export function SkillCard({ language, level }: SkillCardProps) {
  return (
    <div>
      
    </div>
  )
}